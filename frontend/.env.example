# Leave empty in local Vite development to use the /api proxy from vite.config.js.
# Set this only when the frontend is deployed separately from the backend.
VITE_API_BASE_URL=
VITE_DEV_API_TARGET=http://127.0.0.1:8000
